<Window x:Class="DriverScanTester.Views.SearchAddressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Search Address" Height="300" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Data Type" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <RadioButton Content="Byte" IsChecked="{Binding IsByte}" Margin="10,5"/>
                <RadioButton Content="Short" IsChecked="{Binding IsShort}" Margin="10,5"/>
                <RadioButton Content="Int32" IsChecked="{Binding IsCLong}" Margin="10,5"/>
                <RadioButton Content="Int64" IsChecked="{Binding IsLong}" Margin="10,5"/>
            </StackPanel>
        </GroupBox>

        <ToggleButton Grid.Row="1" Content="Enable Numpad Scanning" 
                      IsChecked="{Binding IsNumpadEnabled}" 
                      Height="40" Margin="0,0,0,10"
                      FontSize="16" FontWeight="Bold"/>

        <UniformGrid Grid.Row="2" Columns="2">
            <Button Content="First Scan (Num 1)" Command="{Binding FirstScanCommand}" Margin="5" Background="#FFEBFFEB"/>
            <Button Content="Increased (Num 8)" Command="{Binding IncreasedCommand}" Margin="5"/>
            <Button Content="Decreased (Num 2)" Command="{Binding DecreasedCommand}" Margin="5"/>
            <Button Content="Same as Original (Num 0)" Command="{Binding SameAsOriginalCommand}" Margin="5"/>
            <Button Content="Same as Before (Num 5)" Command="{Binding NotChangedCommand}" Margin="5"/>
            <Button Content="Changed (Num 4/6)" Command="{Binding ChangedCommand}" Margin="5"/>
            <Button Content="Not Changed (Num 5)" Command="{Binding NotChangedCommand}" Margin="5"/>
            <Button Content="Show Results (Num 7)" Command="{Binding ShowMatchesCommand}" Margin="5" FontWeight="Bold"/>
            <Button Content="Close Results (Num 9)" Command="{Binding CloseMatchesCommand}" Margin="5"/>
        </UniformGrid>
    </Grid>
</Window>
